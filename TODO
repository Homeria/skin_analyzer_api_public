1. Schemas(dtos)
    1. 요청 스키마 : 변경 없음
    2. 응답 스키마
        나이 & 질병 -> 컨디션(ex.건조) & 질병
        결과가 classification? percent?
        모델 팀이랑 회의 필요 

2. service 추가 및 수정
    1. service 종류 :  나이 & 질병 -> 컨디션(ex.건조) & 질병
    2. torch 및 cuda 사용에 맞게 repo 수정 후 구현 필요

3. validator : 검증 및 가공
    1. request_validator : 일정 부분 구현 완료
    2. analysis_validator : 서비스 로직에 맞게 추가 구현 필요
       
4. repository
    1. torch 및 cuda 패키지 설치 (requirements.txt 추가 필요)
    2. repo 종류 : 나이 & 질병 -> 컨디션(ex.건조) & 질병
    3. 모델 사용 위한 데이터 전처리 로직 추가 필요 - 서비스 또는 레포에서 처리

5. controller
    1. validator 활용하여 코드 정리
    2. service 및 repository 변경에 맞게 수정
    3. 응답 스키마에 맞게 결과 조합

6. AI Model
    1. 파일 목록 : model.pt, model_class.py
    2. 필요 라이브러리 : torch, torchvision, CUDA, ...
    3. Input 규격 : 이미지 크기, 정규화 방식, 텐서 모양 등
    4. Output 규격


    

